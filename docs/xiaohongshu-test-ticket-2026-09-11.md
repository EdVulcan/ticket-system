# 一分钱多检票点测试票配置记录

> **当前决定（2026-09-11）**：用户取消一分钱测试票方案，改用正常价格商品测试，不再修改或重新提交该测试商品审核。本轮推送审核原因兼容修复；核销后退票仍须由履约供应商初始管理员通过后台纠错退款流程执行，游客自助不开放已使用退款。

用户授权在正式后台创建并上架一张一分钱、多检票点测试票。通过管理后台正常流程完成，未直接改数据库、未购买或核销门票。

- 商户：SYS001，乳源县云门山旅游景区开发有限公司。
- 线上票种：小红书多点核销测试票，后台编号 11。
- 景区：韶关云门山。
- 售价/结算价：0.01 元；小红书映射售价也是 0.01 元。
- 发码：一票一码（一人一张），支持多份测试订单。
- 库存：总库存 20 张。
- 有效期：后台设置为购买后 7 天有效。
- 核销规则组：多点核销联调，组内可选点位数 0（全部点位），景区大门、上程索道、下程索道分别可核销 1 次。
- 退款规则：未核销可退（随时退）；任一点使用后不允许游客自动退款。
- 未启用供应商分销、未设置上游供应，也未将该新映射加入随机立减活动。

小红书渠道沿用现有 `xhs-sandbox` 编码，但后台显示“正式启用”，并非新建沙盒账号。外部商品编码为 `XHS_TEST_MULTI_20260911`，SKU 为 `XHS_TEST_MULTI_20260911_SKU`；选择官方“景区门票”类目、云门山景区门店、团购券、总部结算。商品图为本次生成的“联调测试专用”图片，说明明确列出三个检票点和未使用退款条件。

结果：本地票种已上架，商品映射已启用，发布资料及图片保存成功，平台已接收同步提交。2026-09-11 15:31:38 主动查询官方审核返回“审核未通过，商品不可售”，页面未返回具体退回原因。未把提交成功写成审核通过，也未绕过审核闸门。

已重新打开线上票种编辑页，只读核对保存结果：三个检票点均为 1 次，组内可选点位数为 0（全部点位）；退票类型为“未核销可退（随时退）”。核对后取消退出，未再次保存或更改配置。

用户已登录 AppID `672997dc4889610001aa80b2` 的小程序开放平台。交易能力页面确认已开通，页面要求通过本地生活商家平台管理门店和账户；“立即认领门店”跳转的商家平台仍显示登录页，登录状态不互通。已请用户登录该页面，继续查找商品审核详情；尚未确认商家平台是否展示本商品及具体退回原因，不推断价格或测试名称是拒绝原因。

当前具体退回原因已经通过下文的服务器诊断确认：“无意义商品”。原因展示修复尚未部署；商品本身仍须按平台要求处理，审核通过前小程序不会展示这张可购买测试票。

## 驳回原因诊断补充

官方商品 get 文档（DC371309）有可选 `audit_info.reject_reason`；当前代码读取该字段存为 `audit_message`，管理端已有展示。不能仅凭页面为空认定平台未返回原因。待核对三项：平台查询是否缺原因、已加密保存的 PRODUCT_AUDIT 回调字段形态是否匹配解析器、后续空查询是否覆盖回调原因。

只读脚本 `tmp/xhs-product-audit-readonly.py` 已上传到服务器 `/home/codexdiag/xhs-product-audit-readonly.py`，限定 SYS001 / 渠道 2 / 商品 11 / `XHS_TEST_MULTI_20260911`。数据库使用只读事务；在服务器内解密近期商品审核回调，仅输出匹配商品的审核字段；再调用官方 token 和商品 get 查询，不提交商品、不改业务数据、不输出密钥或 token。语法与服务器 Python 依赖已检查，非 root 执行会在读取配置前拒绝。

当前 codexdiag / ticket-deploy SSH 可连接，但无读取运行密钥所需权限，历史临时 root 密码认证失败。已请用户执行准备好的命令或更新服务器凭据。真实回调和平台响应尚未取得，不进行推测性代码修改。诊断按 diagnose 技能先取得可复现输入，渠道边界沿用 scenic-ticketing-platform 技能。

### 用户返回的真实诊断结果

用户随后在 root 终端完成只读诊断，确认映射 8 / 商品 11。回调事件 19、20 都已验签、解密并保存，字段为 `Status=2`、`AuditTime`（分别 1789111820、1789111852）、`RejectReason=无意义商品`。现有解析器只取 `RejectTime`，所以两个事件都进入 manual_review，原因未投影到商品配置。官方 get 返回 `audit_status=REJECT`，完全没有 `audit_info`，因此查询不能补回原因，原本保存的原因也可能被空详情覆盖。

2026-09-11 再次读取 DC192939，官方文档仍列 `RejectTime`，实际生产样本为 `AuditTime`；应兼容两者，不取消合法时间、Status=2、商品身份与当前提交时间检查。修复应只恢复已拒绝商品的诊断信息，不能改变不可售状态或把审核中视为通过。不得为绕过“无意义商品”驳回而擅自把测试商品伪装成普通商品；需另行确认符合官方要求的真实体验商品或测试方式。

本地已兼容两种时间字段；主动查询拒绝但缺原因时保留匹配审核信息，必要时从当前提交之后最近 100 条同租户/渠道认证回调恢复该商品最新的匹配拒绝信息，历史回调处理状态不回写。生产样本直接回归在修复前失败（状态/原因/时间为空），修复后通过。边界复核补齐没有提交时间不复用旧资料、查询时间早于本次提交时不写回旧审核时间；按原 CAS 更新，期间重配置或新回调仍优先。

最终验证：`go test ./internal/service -run 'TestXiaohongshu(ProductAudit|Webhook)' -count=1 -v` 全部 8 个顶层测试通过（33.492s），包含真实回调验签解密/幂等、旧字段兼容、恢复最新匹配原因、重复空查询、其他租户/商品、旧提交/旧查询时间、无提交时间、历史回调不改写及已有审核调度/并发保护。`go test ./internal/xiaohongshu -count=1`（0.274s）、该包 `go vet` 和 `git diff --check` 通过。测试使用独立本地 PostgreSQL 和模拟平台查询，不是生产发布验收。两个 Luna Max 子 agent 分别补充回调回归和只读复核，主线程审阅实际差异并完成最终专项。尚未推送部署，正式后台不会因本地修改自动变化；部署后下一次定时/手动审核查询即可从现存回调补回本票原因，无需手工修改数据库。
